export default {
  view: {
    width: window.innerWidth,
    height: window.innerHeight,
    backgroundColor: 0xfafafa,
    worldWidth: 1000,
    worldHeight: 500,
    resizeTo: window,
  },
  assets: {
    root: '/',
  },
};
